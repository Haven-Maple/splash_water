# 固定夜间 IR 成像并拒绝构图守卫

状态：已接受，2026-07-15。

当前部署要求摄像头在夜间固定为红外黑白成像，识别主链只处理稳定的 `day_visible` 与 `night_ir` 图像配置。我们不实现 `night_visible` 或识别运行中的彩色/红外切换处理，因为摄像头对补光与环境变化存在延迟和滞回，单次 probe 不能可靠代表整段采样。此前尝试的构图一致性守卫也已删除：它需要额外维护日夜参考图，增加部署成本，而当前没有重复、可复核的预置点构图漂移证据证明其必要性。只有现场证据显示构图漂移是稳定根因时，才重新评估该守卫与额外标定要求。
